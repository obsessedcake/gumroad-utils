[user]
app_session = ChangeMe
guid = ChangeMe
user_agent = ChangeMe
; Email used to purchase products (required for email confirmation flow)
email = ChangeMe
; Cloudflare clearance cookie (required to bypass Cloudflare protection)
; Get from browser DevTools > Application > Cookies > gumroad.com > cf_clearance
cf_clearance = ChangeMe

[scrapper]
product_folder_tmpl = {purchase_at:%Y.%m} {product_name} ({price})
slash_replacement = -
